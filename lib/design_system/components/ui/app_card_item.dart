import 'package:flutter/material.dart';

class AppCardItem extends StatelessWidget {
  const AppCardItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
